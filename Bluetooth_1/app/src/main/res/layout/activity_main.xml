<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.hasantarek.bluetooth_1.MainActivity">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ON/OFF"
        android:textSize="20dp"
        android:id="@+id/onoff_id"
        android:gravity="center_horizontal"
        />


<Button
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_below="@+id/onoff_id"
    android:text="Discoverable"
    android:textSize="20dp"
    android:onClick="discoverable"
    android:gravity="center_horizontal"
    android:id="@+id/dis_id"
    />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Discover List"
        android:onClick="discoverlist"
        android:textSize="20dp"
        android:gravity="center_horizontal"
        android:layout_below="@+id/dis_id"
        android:id="@+id/disList_id"
        />
    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/list_view"
        android:layout_below="@+id/disList_id"
        android:layout_marginTop="10dp"
        ></ListView>


</RelativeLayout>
